<header>
  <nav>
    <div class="navLogoAndName">
      <a href="/" class="linkBlock">
        <img src="/assets/images/d_logo.min.svg" alt="" class="navLogo" />
        <h1 class="nameSmallScreen">
          <span class="navSurname">j</span><span class="navName">den</span>
        </h1>
        <h1 class="nameBigScreen">
          <span class="navSurname">julien</span><span class="navName"
            >dendauw</span
          >
        </h1>
      </a>
    </div>
    <div class="navLinks">
      <!-- <a href="#contactForm">contact</a> -->
      <a href="/thoughts">thoughts</a>
    </div>
  </nav>
</header>

<style lang="scss">
  header {
    grid-area: header;
  }

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 100%;
    margin: 0 2em;

    .nameSmallScreen, .nameBigScreen {
      margin: 0;
    }

    @media screen and (min-width: 1260px) {
      justify-content: space-around;
      padding: 0; /* We can remove the padding since items will be centered */
    }

    .navLogoAndName {
      .linkBlock {
        display: flex;
        align-items: center;
        text-decoration: none;

        .navLogo {
          display: none;

          @media screen and (min-width: 1260px) {
            display: inline;
            width: 5rem;
            height: 5rem;
            padding: 0.3rem;
            margin-right: 2.5rem;
            border-radius: 1rem;
            border: 0.2rem solid var(--link);
          }
        }

        .nameBigScreen {
          display: none;
        }

        @media screen and (min-width: 1260px) {
          .nameSmallScreen {
            display: none;
          }

          .nameBigScreen {
            display: inline;
          }
        }

        .navSurname {
          color: var(--blue-lite);
        }
        .navName {
          color: var(--accent);
        }
      }
    }

    .navLinks {
      a {
        color: var(--link);
      }

      :not(:last-child) {
        margin-right: 1em;
      }
    }
  }
</style>
